<Window x:Class="Wpf_Slider.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Wpf_Slider"
        mc:Ignorable="d"
        Title="Slider" Height="250" Width="400">
    <StackPanel VerticalAlignment="Center" Margin="20">
       
        <Slider
        x:Name="mySlider"
        Maximum="100" TickPlacement="BottomRight"
         TickFrequency="10" ValueChanged="mySlider_ValueChanged"
         IsSnapToTickEnabled="True"
         Value="0"></Slider>

        <TextBlock x:Name="myTextblock" 
            FontSize="{Binding ElementName=mySlider,Path=Value,
            UpdateSourceTrigger=PropertyChanged}" 
                   >  </TextBlock>
    </StackPanel>
</Window>
